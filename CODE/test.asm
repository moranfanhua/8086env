DATA SEGMENT
    MSG DB 'Hello, 8086!', 0DH, 0AH, '$'
DATA ENDS

STACK SEGMENT STACK
    DW 128 DUP(?)
STACK ENDS

CODE SEGMENT
    ASSUME CS:CODE, DS:DATA, SS:STACK

START:
    MOV AX, DATA
    MOV DS, AX

    MOV DX, OFFSET MSG
    MOV AH, 09H
    INT 21H

    MOV AH, 4CH
    INT 21H

CODE ENDS
END START